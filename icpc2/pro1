#include<bits/stdc++.h>
using namespace std;

int main()
{
    float x;
    cin >> x;
    cout << x << "\n"; 
    if (30 <= x && x <=50)
    {
        if (x > 38.0)
        {
            cout << "1\n";
        }
        else if (37.5 <= x && x <= 38.0)
        {
            cout << "2\n";
        }
        else if (x < 37.5)
        {
            cout << "3\n";
        }
    }
    
    return 0;
}